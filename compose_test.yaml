services:
  dashboard-test:
    image: dunglas/frankenphp:php8.5
    ports:
      - "8090:80"
    volumes:
      - ./:/app/
    entrypoint: >
      bash -c 'for f in /app/tests/*_test.php; do echo "== $$f"; php "$$f" || exit 1; done'
      
